# Optional build-time overrides for the Supabase client.
# These values are PUBLIC (a publishable key + project URL) and safe to ship in
# the client bundle — data is protected by row-level security. The app already
# ships with working defaults baked in (see src/lib/supabase.ts); set these only
# to point a build at a different project.
VITE_SUPABASE_URL=https://pzqdeqexisohgeuzqyal.supabase.co
VITE_SUPABASE_KEY=sb_publishable_ySKE5_0dmyk8Tm8xIT2IJg_L6tnPzKm
